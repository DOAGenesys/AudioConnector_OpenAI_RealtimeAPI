# This is the secret key your application will use to validate incoming connections from Genesys.
# You must configure Genesys AudioHook to send this exact value in the 'x-api-key' header.
GENESYS_API_KEY=<your_shared_secret_with_genesys>

# --- AI Vendor Selection ---
# Choose which AI provider to use: 'openai' or 'gemini'
AI_VENDOR=openai

# --- Common AI Settings (vendor-agnostic) ---
# Model to use (defaults set based on AI_VENDOR if not specified)
# OpenAI: gpt-realtime-mini, gpt-realtime
# Gemini: gemini-2.5-flash-native-audio-preview-09-2025
AI_MODEL=

# Voice to use (defaults set based on AI_VENDOR if not specified)
# OpenAI voices: alloy, ash, ballad, coral, echo, sage, shimmer, verse
# Gemini voices: Kore, Puck, Charon, Aoede, Fenrir, Orbit
AI_VOICE=

# --- Vendor-Specific API Keys ---
# OpenAI API key (required when AI_VENDOR=openai)
OPENAI_API_KEY=<your_openai_api_key>

# Gemini API key (required when AI_VENDOR=gemini)
GEMINI_API_KEY=<your_gemini_api_key>

# --- Debug settings ---
DEBUG=true
