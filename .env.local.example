# This is the secret key your application will use to validate incoming connections from Genesys.
# You must configure Genesys AudioHook to send this exact value in the 'x-api-key' header.
GENESYS_API_KEY=<your_shared_secret_with_genesys>

# --- AI Vendor Selection ---
# Choose which AI provider to use: 'openai' or 'gemini'
AI_VENDOR=openai

# --- OpenAI API Configuration ---
# Required when AI_VENDOR=openai
OPENAI_API_KEY=<your_openai_api_key>
OPENAI_MODEL=gpt-realtime-mini

# Voice options for OpenAI:
#   - alloy, ash, ballad, coral, echo, sage, shimmer, verse
OPENAI_VOICE=sage

# --- Gemini API Configuration ---
# Required when AI_VENDOR=gemini
GEMINI_API_KEY=<your_gemini_api_key>
GEMINI_MODEL=gemini-2.5-flash-native-audio-preview-09-2025

# Voice options for Gemini (auto-selects based on language):
#   - Kore (default), Puck, Charon, Aoede, Fenrir, Orbit
# Note: Set voice in Genesys session variable OPENAI_VOICE

# --- Debug settings ---
DEBUG=true
